{
  "name": "current-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend foundation for CURRENT — elite athlete operating system",
  "scripts": {
    "db:push": "supabase db push",
    "db:pull": "supabase db pull",
    "db:reset": "supabase db reset",
    "types:generate": "supabase gen types typescript --local > supabase/types.ts",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx src/db/seed.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.41.0",
    "postgres": "^3.4.4",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.0",
    "supabase": "^1.200.3",-
    "tsx": "^4.19.2",
    "typescript": "^5.4.5"
  }
}
